# Project manifest for the vibe harness (schema 1). Seeded by
# `vibe init` from the "go" preset; edit freely, then `vibe up`.
schema: 1
harness: {{.HarnessVersion}}
image:
  base: "mcr.microsoft.com/devcontainers/base:debian"
  agents: [claude]
  toolchains: [go]
agent:
  cmd: claude
  tmux: true
  memory: {{.AutoMemory}}
bootstrap:
  required: [git, go]
  auto: {install: true, git_hooks: false, git_lfs: false}
